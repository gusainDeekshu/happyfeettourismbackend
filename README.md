# Hercules
backend/
├── config/
│   └── corsOptions.js      # CORS settings
├── controllers/
│   └── productController.js # Logic for handling requests
├── data/
│   └── db.js               # The Mock Database (Data from your images)
├── routes/
│   └── productRoutes.js    # API Endpoints
├── .env                    # Environment variables
└── server.js               # Entry point
